schema:
  - pkg/graph/schema.graphql

exec:
  filename: pkg/service/graphql_generated.go
  package: service

model:
  filename: pkg/service/graphql_models_gen.go
  package: service

resolver:
  layout: follow-schema
  dir: pkg/service
  package: service

# Static credentials from values.storeCredentials (must match shop common store.credentials).
apiVersion: v1
kind: Secret
metadata:
  name: temporal-cassandra-secret
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  password: {{ .Values.storeCredentials.cassandraSuperuserPassword | quote }}

# Courier Emulation Service

> [!NOTE]
> Сервис эмуляции курьеров для тестовой среды Delivery Boundary. Имитирует поведение курьеров: движение по маршрутам, обновление геолокации, принятие заказов и подтверждение доставки.

## Назначение

Сервис для эмуляции поведения курьеров в тестовой среде. Имитирует движение курьеров, обновление геолокации, принятие заказов и подтверждение доставки.

## Функциональность

- Автоматическое обновление геолокации курьеров
- Эмуляция движения по маршрутам
- Автоматическое принятие назначенных заказов
- Эмуляция доставки заказов
- Изменение статусов курьеров

---

## 🧭 Алгоритм генерации маршрутов для сервиса эмуляции курьеров

### TL;DR

Берём Berlin OSM → делаем OSRM graph → случайные пары точек → OSRM route → сохраняем polyline. Готово. У тебя база маршрутов.

### Шаг 1: Скачиваем Berlin OSM

С сайта Geofabrik:

```bash
wget https://download.geofabrik.de/europe/germany/berlin-latest.osm.pbf
```

### Шаг 2: Строим OSRM Graph (routing engine)

OSRM — это супербыстрый мотор маршрутизации. Он превратит OSM в дорожно-графовую структуру, по которой мы будем генерировать маршруты.

Docker команды:

```bash
docker run -t -v $(pwd):/data osrm/osrm-backend osrm-extract -p /opt/car.lua /data/berlin-latest.osm.pbf
docker run -t -v $(pwd):/data osrm/osrm-backend osrm-partition /data/berlin-latest.osrm
docker run -t -v $(pwd):/data osrm/osrm-backend osrm-customize /data/berlin-latest.osrm
```

### Шаг 3: Запускаем OSRM сервер

```bash
docker run -d --name osrm -p 5000:5000 \
  -v $(pwd):/data osrm/osrm-backend \
  osrm-routed --algorithm=MLD /data/berlin-latest.osrm
```

Теперь API доступно:

```
http://localhost:5000/route/v1/driving/LON,LAT;LON,LAT
```

### Шаг 4: Генерируем N случайных точек

Чтобы генерировать маршруты, нам нужны старт и финиш.

Берём bounding box Берлина из OSM:

- MinLat: 52.3383
- MaxLat: 52.6755
- MinLon: 13.0884
- MaxLon: 13.7610

Генерируем случайные точки:

```go
func randomPoint() (lat, lon float64) {
  lat = rand.Float64()*(52.6755-52.3383) + 52.3383
  lon = rand.Float64()*(13.7610-13.0884) + 13.0884
  return
}
```

### Шаг 5: Запрашиваем маршрут в OSRM

Например:

```
GET /route/v1/driving/{lon1},{lat1};{lon2},{lat2}?overview=full&steps=true
```

OSRM отдаёт:

- polyline маршрута
- distance
- duration
- список манёвров
- координаты
- шаги
- geometry

Пример ответа:

```json
{
  "routes": [
    {
      "distance": 4212.3,
      "duration": 678.9,
      "geometry": "o{`wHyqsbAAW...xyz",
      "legs": [...]
    }
  ]
}
```

### Шаг 6: Сохраняем маршрут в "базу маршрутов"

Например в JSON:

```json
{
  "id": "route_0001",
  "from": [52.520008, 13.404954],
  "to": [52.493820, 13.428290],
  "distance": 4212.3,
  "duration": 678.9,
  "polyline": "o{`wHyqsbAAW....."
}
```

Можно в Postgres, Redis, MinIO — как удобнее.

### Шаг 7: Повторяем 10k раз → получаем красивую тестовую базу маршрутов

Хоть 1k, хоть 50k маршрутов.

По факту:

- 10k маршрутов ≈ 10–20 сек
- 100k маршрутов ≈ 2–3 минуты

OSRM очень быстрый.

### Шаг 8: 🎮 Как использовать это в delivery bot сервисе

Твой бот делает:

```go
assign(routeID)
emit status("courier_assigned")

for each step in polyline:
    sleep(step_duration)
    emit status("courier_moving")
    updateLocation(current_step_coordinates)
    
emit status("courier_at_pickup")
// ... pickup logic ...

for each step in polyline_to_delivery:
    sleep(step_duration)
    emit status("courier_moving")
    updateLocation(current_step_coordinates)
    
emit status("courier_at_delivery")
deliverOrder(packageID, status="DELIVERED")
```

И твой order-service думает, что едет реальный курьер.

---

## Технические детали

### 1. Реализация сервиса эмуляции

- Сервис на Go (аналогично другим сервисам Delivery Boundary)
- gRPC API для управления эмулируемыми курьерами
- Интеграция с Delivery Service для получения назначенных заказов
- Интеграция с Geolocation Service для обновления локаций

### 2. База маршрутов

- Хранение сгенерированных маршрутов (Postgres/Redis/MinIO)
- API для получения маршрутов по ID или случайного маршрута
- Кэширование популярных маршрутов

### 3. Эмуляция движения

- Парсинг polyline в последовательность координат
- Пошаговая эмуляция движения с учетом времени
- Обновление геолокации с заданной частотой (например, каждые 30 секунд)
- Учет типа транспорта для скорости движения

### 4. Стратегии эмуляции

- Всегда принимать заказы (для тестирования диспетчеризации)
- Реалистичные задержки (имитация реального времени доставки)
- Случайные отклонения от маршрута (для реалистичности)
- Обработка ошибок доставки (опционально, для тестирования)

---

## Интеграция с другими сервисами

### Delivery Service

- Подписка на события назначения заказов курьерам
- Эмуляция принятия заказов
- Эмуляция подтверждения доставки

### Geolocation Service

- Автоматическое обновление геолокации курьеров
- Передача координат по маршруту
- Поддержка истории перемещений

### Dispatch Service

- Получение назначенных заказов
- Эмуляция реакции курьера на назначение

---

## Getting Started

TBD: Инструкции по запуску сервиса будут добавлены после реализации.

## ADR

TBD: Architecture Decision Records будут созданы при реализации сервиса.


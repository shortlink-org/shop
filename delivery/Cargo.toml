[package]
name = "delivery"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.12"
prost-types = "0.12"
pbjson-types = "0.6"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tonic = "0.11"
tokio = { version = "1", features = ["full"] }

[build-dependencies]
prost-build = "0.12"


{{/* vim: set filetype=mustache: */}}

{{ include "shortlink-common.ServiceAccount" . }}

{{ include "shortlink-common.Deployment" . }}

{{ include "shortlink-common.Service" . }}

{{ include "shortlink-common.grpc_route" . }}

{{ include "shortlink-common.ServiceMonitor" . }}

{{ include "shortlink-common.PodDisruptionBudget" . }}

{{ include "shortlink-common.NetworkPolicy" . }}

{{/* RequestAuthentication with outputClaimToHeaders (sub → x-user-id) for OMS; see ADR-0005. Replaces shortlink-common.RequestAuthentication so we can set x-user-id from JWT. */}}
